export interface GyuUIOptions {
  components: Array<string>;
}

export enum GYU_UI_BREAKPOINT {
  XS = "xs",
  SM = "sm",
  MD = "md",
  LG = "lg",
  XL = "xl"
};

export enum GYU_UI_SIZE {
  MINI = "mini",
  SMALL = "small",
  MEDIUM = "medium",
  LARGE = "large",
};

export enum GYU_UI_KIND {
  DEFAULT = "default",
  PRIMARY = "primary",
  SECONDARY = "secondary",
  WARNING = "warning",
  ERROR = "error",
};
